// @ts-ignore
import { version, description, name } from "../../../package.json"

export default {
    openapi: "3.0.0",
    info: {
        title: name,
        description,
        version,
    }
}
